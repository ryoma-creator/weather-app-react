
From Nov 25


Nov 25 

    ブラインスピード　サプリメント

    モチベーションビデオを見る

    ポートフォリオサイト
        - Leanrning Journey　
            - 3月からAIへ現在まで１ヶ月単位で要約
            - descriptionの文章を考察
            - レイアウトで幅高さ均一になっていない箇所の修正 (文章量で変わっていたので)
            - 
        - contact 
            - back end が必要なことを知る。
            - htmlfor / bootstrap /
             - https://www.youtube.com/watch?v=ERo_JwWohoQ
             - ディレクトリはどこへ？next jsではどこにファイルを作成すべきか
            - APIエンドポイントについて復習
            - 個人情報→ envファイルの作る場所について
            -   transporter.sendMail(toHostMailData, function (err, info) {の第二引数について
                フォーム要素の属性（name, type）の役割
                イベントオブジェクト（e）の構造と使い方
                非同期処理（async/await）の基本概念
                APIルートの構成と命名規則
                Next.jsのファイル構造規約
                POSTリクエストの基本
                エラーハンドリング（try/catch）の概念
                フォームデータの送信フロー
                サーバーとクライアントの通信の基礎
            
        - ポートフォリオの項目の修正
            - Compareというcomponentが画像のみしか比較できないので、
            それ以外のものでも実装できないかAIに尋ねて、何度も再構築
            - 実装はできた。しかし、画像のサイズが小さい。そこを続きから行いたい。

    ジム
        - ランニング 10ふん
        - バイク　2ふん　10

    TOEIC
        - 30 総合問題

    Motivation video
        - 不平等な力

　　　

    このままだとサプリメントが間に合わない。
    スタミナとか考えてられない。ダメならそれまでだったとしかいない。やるしかない。

    モチベーションビデオを見た後、ランニング時間が通常の2倍になっている。脳内にも声が聞こえる。辛い時に。そこで量が増えて引っ張ってくれていた。

